/**
 * @deprecated Importar desde `@/lib/permissions` (fuente única).
 * Se mantiene este archivo para compatibilidad con imports existentes.
 */
export {
  canAccessChat,
  canAccessComidaPersonal,
  canAccessCocinaCentral,
  canAccessCostes,
  canAccessCostesPersonales,
  canAccessCuentaSeguridad,
  canAccessEscandallos,
  canAccessFinanzas,
  canAccessInventario,
  canAccessPedidosByRole,
  canAccessTeamManagement,
  canAccessMermasExecutiveAnalytics,
  isRouteBlockedForRole,
} from '@/lib/permissions';
