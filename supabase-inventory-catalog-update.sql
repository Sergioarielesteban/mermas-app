-- =============================================================================
-- Inventario: UPDATE en catálogo (obsoleto si usas catálogo por local)
-- =============================================================================
-- Las políticas de UPDATE quedan definidas en supabase-inventory-catalog-per-local.sql
-- (solo el local actual). Si aún no migraste a catálogo por local, puedes aplicar
-- temporalmente las políticas amplias de abajo (no recomendado en producción multi-local).
-- =============================================================================

-- Ver supabase-inventory-catalog-per-local.sql para el modelo actual recomendado.
