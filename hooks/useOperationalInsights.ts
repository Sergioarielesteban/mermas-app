'use client';

/**
 * Alias orientado a producto: mismo hook que patrones temporales (insights operativos locales).
 */
export { useTemporalPatterns } from '@/hooks/useTemporalPatterns';
export { useTemporalPatterns as useOperationalInsights } from '@/hooks/useTemporalPatterns';
